name: crud_estudante_disciplina
description: "CRUD basico com Estudante, Disciplina e Cursando."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.2.3+1
  sqflite_common_ffi: ^2.3.0
  path: ^1.8.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
